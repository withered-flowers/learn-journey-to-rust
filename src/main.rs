mod x01variables;
mod x02numbers;
mod x03char_bool_unit;

// Fix the error below with least amount of modification to the code
fn main() {
    x01variables::invoker();
    x02numbers::invoker();
    x03char_bool_unit::invoker();
}
